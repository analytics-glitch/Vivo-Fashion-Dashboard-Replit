Both go to the Replit agent. Here's the detailed brief — copy everything in the block below and paste it into the Replit agent chat.

Before that, one thing I need you to get from your Neon dashboard (the agent can't access it): the connection limit and the pooler string. In Neon console → your project → Connection Details → look for the "Pooled connection" toggle. It gives a host with -pooler in it. Note both the pooled connection string and your compute's connection limit. Give those to the agent when it asks, or paste them into the brief where marked.

BRIEF FOR REPLIT AGENT — Dashboard performance: worker scaling, DB pool sizing, and Style Tracker on-demand loading

Context: This is a FastAPI + uvicorn BI dashboard (api_pg.py) on a Reserved VM (gce), 8 CPU cores, 15GB RAM, backed by Neon PostgreSQL in production and a local helium PG in dev. It currently runs a SINGLE uvicorn worker, so only 1 of 8 cores serves all traffic. With 16+ concurrent users, heavy endpoints (customer analytics, product analysis, range mgmt) queue behind the GIL and everything feels slow. Do NOT change any business logic, SQL definitions (SOR, tiers, NOOS), or query semantics — this is purely an infrastructure + loading-pattern task. Preserve all existing behavior.

TASK 1 — Multi-worker uvicorn (biggest win).
The production API is launched by watchdog.py via API_CMD (around line 94), currently:

python
API_CMD = [sys.executable, "-m", "uvicorn", "api_pg:app", "--app-dir", ROOT, "--host", "0.0.0.0", "--port", str(API_PORT)]

Change it to run multiple workers, with the count from an env var so it's tunable without a code change:

python
_API_WORKERS = os.environ.get("API_WORKERS", "4")
API_CMD = [sys.executable, "-m", "uvicorn", "api_pg:app", "--app-dir", ROOT, "--host", "0.0.0.0", "--port", str(API_PORT), "--workers", _API_WORKERS]

Default 4 workers (leaves cores for the sync loop + rollup subprocess the watchdog also supervises). IMPORTANT: confirm the watchdog's port-guard/free_port logic and any single-process assumptions still work with --workers (uvicorn will fork; the parent binds the port). The dev launcher run_api.py should stay single-worker (dev doesn't need it).

TASK 2 — Make the DB pool worker-aware so N workers don't exhaust Neon.
api_pg.py line ~250 has MAX_DB_CONNECTIONS = 20. With multiple workers, the pool is created PER worker process, so total connections = workers × MAX_DB_CONNECTIONS. Neon has a connection ceiling. Make the per-worker pool size derive from a total budget:

python
_TOTAL_DB_BUDGET = int(os.environ.get("TOTAL_DB_CONNECTIONS", "32"))
_API_WORKERS_N = int(os.environ.get("API_WORKERS", "4"))
MAX_DB_CONNECTIONS = max(4, _TOTAL_DB_BUDGET // _API_WORKERS_N)  # e.g. 32//4 = 8 per worker

So 4 workers × 8 = 32 total connections. [NEON CONNECTION LIMIT: _____ — fill from Neon dashboard; keep total ≥ 20% below it. Also account for the sync loop + rollup subprocess which open their own direct connections.] If unsure, keep total at 32 or lower.

TASK 3 — Cap FastAPI's sync-endpoint threadpool to match the DB pool (prevents thread/connection starvation).
FastAPI runs sync def endpoints in a default 40-thread AnyIO pool, but there are only MAX_DB_CONNECTIONS per worker — so excess threads block holding requests hostage while waiting for a connection. Cap the threadpool to the pool size. Add to the app's startup (use the lifespan pattern if present, else the existing startup hook):

python
from anyio import to_thread
# in startup/lifespan:
to_thread.current_default_thread_limiter().total_tokens = MAX_DB_CONNECTIONS + 2

Verify this runs inside each worker process after fork, not just in the parent.

TASK 4 — (If pooler string provided) switch production to Neon's pooled endpoint.
[NEON POOLED CONNECTION STRING: _____ — fill from Neon dashboard "Pooled connection".] If provided, the production DATABASE_URL should use the -pooler host (PgBouncer, transaction mode). This removes per-connection cold-start latency and lets workers share Neon's connection budget. CRITICAL caveats for PgBouncer transaction mode: (a) it does NOT support session-level features like SET, server-side prepared statements, or advisory locks that span transactions — the code uses pg_try_advisory_lock for rollup refresh (around line 4020) and conn.set_isolation_level(0) for VACUUM; those run in the sync loop / watchdog which should keep using the DIRECT (non-pooled) connection, NOT the pooler. So: API request path → pooler; sync loop, rollup refresh, VACUUM, migrations → direct connection. Keep them as separate env vars (e.g. DATABASE_URL = pooler for the API, DATABASE_URL_DIRECT = direct for background jobs). If this separation is too risky to get right now, SKIP Task 4 and just do 1–3.

TASK 5 — Style Tracker board: on-demand ("display on click") detail loading.
The Style Tracker page (artifacts/vivo-bi/src/pages/StyleTracker.jsx) calls /api/style-tracker/board on load, and the board is slow. Investigate whether style_tracker_board() (api_pg.py ~line 33645) or the frontend is doing per-style work up front. The page already has per-style endpoints /style-tracker/styles/{id}/warehouse-pct and /style-tracker/styles/{id}/fulfillment that are called lazily — verify the board endpoint itself isn't running expensive per-style subqueries (warehouse %, fulfillment, SOR) for every card on initial load. If it is, defer those heavy per-style computations so the board returns fast with just the essential card fields (style name, status, week, brand, category), and load the heavy detail (warehouse %, fulfillment progress, SOR) only when a user CLICKS a card to expand it — via the existing per-style endpoints or a new /api/style-tracker/styles/{id}/detail endpoint. The board's initial payload should be lightweight. Measure board endpoint time before/after.

TESTING (must do before considering done):

After Tasks 1–3: restart via watchdog, confirm the API binds and serves, confirm 4 worker processes exist (ps aux | grep uvicorn shows 1 parent + 4 workers), and confirm total DB connections to the database stay within budget (SELECT count(*) FROM pg_stat_activity;). Load several heavy pages concurrently and confirm they no longer serialize.
Confirm the sync loop, rollup refresh, and VACUUM still work (they must keep using a direct connection if Task 4 is done).
After Task 5: confirm the Style Tracker board loads fast and card detail still appears on click.
Watch for any "too many connections" or "remaining connection slots" errors from Neon — if seen, lower TOTAL_DB_CONNECTIONS.

Rollback safety: All worker/connection counts are env vars, so they can be tuned or reverted without code changes. If anything destabilizes, set API_WORKERS=1 to return to current behavior.

Do NOT: change any SQL, business logic, tier/SOR/NOOS definitions, caching TTLs, or the VACUUM-after-rebuild logic already in place. This task is infrastructure + the Style Tracker loading pattern only.