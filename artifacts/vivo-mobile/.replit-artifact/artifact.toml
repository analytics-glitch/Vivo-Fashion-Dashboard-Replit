kind = "mobile"
previewPath = "/mobile/"
title = "Vivo BI Mobile"
version = "1.0.0"
id = "artifacts/vivo-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
ensurePreviewReachable = "/status"
name = "expo"
paths = [ "/mobile/" ]
localPort = 20838

[services.development]
run = "pnpm --filter @workspace/vivo-mobile run dev"


[services.env]
PORT = "20838"
BASE_PATH = "/mobile/"
