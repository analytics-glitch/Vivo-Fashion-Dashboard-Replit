Vivo brand assets — upload point
=================================

vivo-logo.svg — the official Vivo wordmark (Century Gothic). If the team
exports the real outlined-path SVG from brand files, drop it here with
this exact name. The app header currently renders the wordmark with the
Century Gothic font stack ('Century Gothic', Questrial, Didact Gothic,
Poppins) which matches closely on every platform; swapping in the real
SVG is optional polish, not required.
